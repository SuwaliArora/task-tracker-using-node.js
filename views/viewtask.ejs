<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager - View Tasks</title>
</head>
<body>
    <h1>Task Manager - View Tasks</h1>

    <% if (tasks.length > 0) { %>
        <h2>Tasks</h2>
        <ul>
            <% tasks.forEach(task => { %>
                <li><strong>Title:</strong> <%= task.title %>, <strong>Status:</strong> <%= task.status %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No tasks available.</p>
    <% } %>

    <a href="/tasks">Create New Task</a>
</body>
</html>